// 174 로그함수

import kotlin.math.*

fun main(args: Array<String>)
{
    println(log(4f, 2f)) // 로그 2f의 4f 값을 반환한다.
    println(log2(8f)) // 로그 2의 8f 값을 반환한다.
    println(log10(10000f)) // 로그 10의 10000f 값인 4.0을 반환한다.
    println(ln(E))
    println(ln1p(E)) // 로그 e의 E - 1 값을 반환한다.
}